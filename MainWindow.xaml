<Window
    x:Class="AutomatonEditor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Edytor Automatów"
    Width="800"
    Height="600">
    <Grid>
        <ItemsControl x:Name="StatesControl"
                      ItemsSource="{Binding States}"
                      MouseLeftButtonDown="CanvasMouseLeftButtonDown">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas Background="White"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemContainerStyle>
                <Style TargetType ="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid
                        Width ="50"
                        Height="50"
                        MouseLeftButtonDown="State_MouseLeftButtonDown"
                        MouseLeftButtonUp="State_MouseLeftButtonUp"
                        MouseMove="State_MouseMove">

                        <Ellipse
                            x:Name="Circle"
                            Fill="White"
                            Stroke="Black"
                            StrokeThickness="2"
                            >

                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Stroke" Value="Black"/>

                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="Stroke" Value="Red"/>
                                            </DataTrigger>
                                    </Style.Triggers>
                            </Ellipse.Style>
                        </Ellipse>

                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontWeight="Bold"
                            Text="{Binding Name}"
                            />

                        <Grid.Style>
                            <Style TargetType="Grid">
                                <Style.Triggers>
                                    
                                </Style.Triggers>
                            </Style>
                        </Grid.Style>
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
<<<<<<< HEAD

=======

>>>>>>> 4608bdaf0314368fd9aa635c4b0457c9661b8291
        </ItemsControl>
    </Grid>
</Window>